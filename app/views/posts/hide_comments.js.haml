$(document.getElementById(#{@post.id})).children(".comments").remove();
document.getElementById('link-show-#{@post.id}').style.display = 'block';

var container = document.querySelector('#masonry');
var msnry = new Masonry(container, {
columnWidth: '.post',
itemSelector: '.post'
});